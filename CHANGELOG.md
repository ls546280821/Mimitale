# Mimitale（原 Barbara，初名 Cyrene）优化更新日志

> 本文是**历史记录**，正文里的旧名（Barbara / Cyrene）按当时的叫法保留，不做追溯改名。
> 当前名字：**Mimitale**（中文名「如我所书」）。

> 注：2026-09 界面模块化的 13 个重构批次记录（第一~十三批）已从本文件移出 ——
> 重构已收尾，那些施工过程记录不再有对照价值。

## 2026-09 状态面板：类型 / 范围 / 进度条 / 分组 / 剧情选项

状态面板原来是「一堆 `字段：值` 的字符串」。这次给它补上了**类型和约束**，
顺手把导入他人卡片时那份设定（对方的「互动模板」）也接了过来。

### 字段现在有类型、范围和变化规则

```
好感度  [ 63 ] /100
        ▓▓▓▓▓▓░░░░        ← 数值字段自动画进度条
```

- **类型**：文本 / 数值 / 列表
- **范围**：填了范围之后，模型写 `150/100`、`-5/100` 都会被**程序拉回**范围内
  （你在面板里手填也一样），而且保留原写法（分母不动）
- **变化规则**：一段给模型看的话，原样注入。比如「按本轮剧情在当前值基础上增减，
  单轮变化不超过 10」；纯文本字段也能用（「关系阶段：低于 30 陌生，30~59 熟人…」）

这三样和范围一起拼成一段图例注入给模型，并明确告诉它「超范围会被程序拉回」。

### 字段可以分组

属性里填同一个「分组」名，面板里就归到一块，各带小标题（「关系」「背包」）。
分组按你填的顺序排，没分组的排最后。

> 分组小标题在提示词里**刻意写成「—— 关系 ——」而不是「【关系】：」**：
> 后者会被状态栏自己的解析器当成一个名叫「关系」的字段，于是 AI 照抄、
> 下一轮就凭空多出一个垃圾字段。

### 剧情选项：每轮给几个可点选项

角色编辑器里勾一下，这个角色的会话就每轮多出一块可点选项，**点一下当作你说这句话
发出去**（1~6 个可调，可加语气要求）。

和「帮我想想」的区别：那是**额外发一次请求**、一次性的；剧情选项跟状态栏**同一次
回复**一起给出，零额外请求、每轮更新、常驻在面板里。

### 导入他人卡片：「互动模板」自动变成属性

有些角色卡网站把字段定义写进 `extensions.status_template`
（`{key, label, type:'meter', min, max, initial, hint}`）。导入时现在会：

- 把每个字段映射成 Barbara 的角色属性（类型/范围/规则/初始值全带过来）
- 把它的 `template` 翻译成分组（`status_bar` → 「状态栏」、`relationship` → 「关系」）
- 列表型初始值（`["衣服"]`）拍平成 `衣服`

### 🐛 顺带修掉的

- **v3 变体卡的开场白整个丢失**：这类卡没有 `first_mes`，开场白放在 `chat_history`
  里。以前只读 `first_mes`，导进来开场白是**空的**
- **导入后绑定指向不存在的书**：渲染层给角色和世界书重发 id 时，没改写角色里的
  `worldbookIds` —— 书在库里、但永远不生效，全程不报错
- **模型自己冒出来的数值字段没有进度条**：从值的形状推断（`63/100` 的分子是当前值、
  分母是满值），只认这一个形状，且不推断下限（分母只能告诉我们上限）
- 数值字段的存储格式统一成 `分子/满值`（卡里 initial 常是裸数字 `20`，统一后
  面板上才看得出满值）
- `.panel-head` / `.panel-title` 被「设置弹窗的区块头」和「状态面板的头」共用，
  两组规则互相覆盖（gap 10→8、margin-bottom 14→8、标题 13→12.5px）——
  弹窗那组改名成 `.section-*`

### 🧪 冒烟测试 253 → 431 条

新增覆盖里最值钱的两条：**真卡的原始字节**作为回归样本直接喂真导入链路
（`tools/fixtures/`）；以及**导出 → 真导入**的往返 —— 拿渲染层交出来的真实 PNG
字节读回来，验证书和绑定都还在。

另外加了 `--shot=<场景>` 截图能力，用来肉眼核对间距/对齐/配色这类结构测试
盖不住的东西：

```bash
npx electron tools/smoke-test.js --no-sandbox --shot=panel
```

---

## 2026-09 生图 / 向量模型改成跟着服务商走的下拉

### 之前是个自由输入框

「生图模型」和「向量模型」原来都是文本框，得自己把模型名敲进去 —— 而模型名
本来就在服务商的「可用模型」列表里躺着，敲错一个字就是一次莫名其妙的报错。

现在两个都是**下拉框，跟着上面选的服务商走**：

```
生图服务商  [画图服务 ▾]
生图模型    [dall-e-3 ▾]      ← 列表 = 画图服务的可用模型
```

换服务商时模型下拉跟着重填。

### 三个边界情况都处理了

- **保存过的模型不在列表里**（换过服务商、或者列表被人删过）→ 补一个
  「`xxx`（不在列表里）」的选项摆在那儿。不加这一手的话，一打开设置就会被**静默改掉**
- **用户主动换服务商** → 不从老模型里继承（那个名字在新服务商这儿没有意义），直接选新列表的第一个
- **服务商还没填模型** → 下拉禁用 + 一句「先在左边选一个服务商」，不给你一个空框

想加列表里没有的模型，就去「服务商」那一节加一行（或点「拉取可用模型」）——
**模型列表只有一份**，聊天、生图、向量共用它。

### 🧪 冒烟测试 251 → 253 条

两条新的：生图模型确实是个 `<select>` 且跟着服务商填好、向量模型同理。

> 顺带修了测试自己的一个坑：`setValue` 只派发 `input`，而真实浏览器里**选下拉会同时触发
> `input` 和 `change`**。少了 `change` 就测不到「换服务商 → 模型下拉重填」这件事
> （那 4 条失败全是这个原因，不是代码的问题）。现在对 `<select>` 会把两个事件都派发。

---

## 2026-09 语义检索（⑦ RAG）

### 🔍 世界书靠关键词命中，这是个死角

你写了「十二泰坦」的详细设定，但对话里说的是「那些神」—— 关键词永远命中不了，
那条设定就永远出不来，模型只能自己编。

现在开启语义检索之后，每一轮会按**意思**去找相关的旧内容和设定，捞到的作为一段
`[可能相关的往事]` 注入提示词。

### 第三组独立配置

| | 接口 | 配在哪 |
| --- | --- | --- |
| 聊天 | `/chat/completions` | 设置 → 服务商 |
| 生图 | `/images/generations` | 设置 → 生图 |
| **向量** | `/embeddings` | 设置 → **语义检索** |

`ragEnabled` / `embeddingProviderId` / `embeddingModel` —— 又是三个独立字段。
默认**关**：开着每轮都要多调一次接口，第一轮还要给历史补索引，得让用户明确知道在花这份钱。

### 几个设计决定

- **不硬凑**：`minScore` 门槛（默认 0.32）。语义检索最怕「不管相不相关都塞几条进来」，
  上下文被污染了比不检索还差
- **最近几条不捞**：它们本来就会进上下文，捞回来是白占位置
- **只捞这个会话绑定了的世界书**，不会把别的书串进来
- **缓存键里带模型名**（`<model>::<key>`）：不同 embedding 模型算出来的向量根本不可比，
  带上模型名之后换模型自然就不会串味
- **增量索引**：一轮最多补 32 条，长对话分几轮慢慢补完，不卡聊天
- **向量存 base64 的 Float32**：1536 维按 JSON 数字存约 20KB，按 Float32 是 6KB ——
  而 `vectors.json` 是每次改动整份重写的
- 用**余弦**而不是欧氏距离：这样不用管各家服务商归一化没归一化

### 🧩 抽了 `main/vectors.js`

编解码、余弦相似度、topK 排序、候选收集全在里面，都是**纯函数**（不碰网络不碰文件）
—— 测试能 require 同一份实现。假后端的 `rag:recall` 也直接调它，
所以「问『那些神』→ 捞出写了『十二泰坦』的设定」这件事是**真排序算出来的**。

### 🐛 测试抓到一个真 bug

「候选收集跳过最近几条消息」红了：`collectCandidates` 原来是对**原始数组**尾部切片，
而「最近的几条」应该按**可用消息**算。数组里还有 error 消息和空白消息
（生成失败就会留一条），直接切的话跳过的那几条会偏 —— 结果「最近的内容」会被
当成往事捞回来。改成先过滤再切。

### 🧪 冒烟测试 227 → 251 条

- **向量单测 11 条**：编解码往返、base64 确实更省、坏数据返回 null、
  余弦（同向 1 / 正交 0 / 反向 -1）、**没归一化也算得对**、维度不一致返回 0、
  topK 与阈值、exclude、**全都不够像时宁可一条都不给**、内容指纹
- **候选收集 3 条**：跳过最近几条 / 只捞绑定的那本书 / error 与空白不收
- **注入链路 5 条**（界面 + 宿主）：配好落盘且聊天模型没被动过、
  相关的被捞回来、**不像的没被硬凑进来**、注入的是独立 system 消息、
  **关掉之后就不再注入**（负向对照）、检索用的是「向量」那组配置

> 这些功能**界面上没有可见反应**（RAG 是「悄悄多给模型一段背景」），
> 所以宿主侧那几条才是真正在验它 —— 光看界面什么都看不出来。

---

## 2026-09 给剧情配插画（⑥b 生图）

### 🎨 生图和聊天是两个模型，所以配两组

| | 接口 | 配在哪 |
| --- | --- | --- |
| 聊天 | `/chat/completions` | 设置 → 服务商 |
| **生图** | `/images/generations` | 设置 → **生图**（独立一组） |

`settings.imageProviderId` / `imageModel` / `imageSize` —— 和聊天的
`activeProviderId` / `activeModel` **完全没有关系**。这正是你之前担心的那件事：
**换生图模型不会让任何一段对话改变风格**（每个会话自己记着用哪个聊天模型）。

设置里那段说明直接把这件事写出来了：生图和聊天是两个模型、通常在两个端点上、
服务商可以和聊天共用，以及目前只支持同步返回图片的接口（通义万相那种异步任务还不支持）。

### 交互

鼠标悬停在 AI 回复上 → **「配图」**：

- 提示词取自那条回复的正文（剥掉状态栏和 markdown 标记，截 800 字）
- 画的时候整条消息轻轻呼吸
- 画好直接贴在那条回复**上方**，点开能看大图
- **图会先压一档再存**（最长边 1024、webp）：生图接口给的通常是 1~2MB 的 PNG，
  原样塞进 `conversations.json` 的话，几张图就是几十 MB

**没配生图就不显示「配图」按钮** —— 免得点了才知道没配。

### 两个实现细节

- **有的服务商无视 `response_format` 直接给图片链接**。渲染层 CSP 是 `img-src 'self' data:`，
  外链根本加载不了 —— 所以在主进程把图下载回来转成 dataURL。`downloadBinary()` 顺带处理了重定向
- 生图比聊天慢得多，超时给到 **120 秒**（`requestJson` 默认是 30 秒）

### 🐛 测试抓到一个真 bug

新场景跑到「配好之后出现『配图』」时红了：**保存设置后没有重绘消息**。
`saveSettings()` 只重绘了顶栏和模型下拉，而「配图」按钮的有无取决于 `imageProviderId` ——
于是配好生图之后，界面上不会出现按钮，非得切个会话才出来。

消息里还有别的东西跟着设置走（日期分隔看 `showDate`），所以修法是保存设置后
补一次 `renderMessages({ forceScroll: false })`。

### 🧪 冒烟测试 216 → 227 条

界面侧 8 条，走的是完整的一条路：没配 → 没有按钮 → 去设置里配一组 →
配置落盘且**聊天模型没被动过** → 按钮出现 → 点它 → 图挂到那条消息上 → 落盘且是 dataURL。

宿主侧 3 条：**用的是「生图」那组配置而不是聊天模型**（用错了界面照样出图，
但你的对话模型会被当画图模型去打 `/images/generations`）、提示词取自那条回复的正文、
请求里带上了尺寸。

---

## 2026-09 给 AI 看图（⑥a）

### 🖼 三种加法

输入框左边多了个图片按钮。除了点它选文件，还支持：

- **Ctrl + V 粘贴** —— 截图之后直接贴，比存文件再选快得多
- **拖进来** —— 拖到输入区时输入框会高亮，告诉用户「往这儿放」

加进来的图变成输入框上方的缩略图，每张右上角有 ×。**只发图不打字也行** —— 问「这是什么」不一定非要配一行字。

### 发给模型的是多模态数组

`message.images = [dataURL, ...]`，发请求时把这条消息的 `content` 从字符串换成：

```js
[{ type: 'text', text: '…' }, { type: 'image_url', image_url: { url: 'data:image/webp;base64,…' } }]
```

这是 OpenAI 那套的通用写法，Claude / Gemini 的兼容层一般也认。

**没带图的消息仍然是纯字符串** —— 不能为了省事把所有请求都改成数组，一些便宜的老接口收到数组会直接报 400。这条也写进测试了。

### 几个决定

- **图片先压到最长边 1024 再发**。视觉模型内部一般也就缩到这个量级，传原图只是白烧 token 和流量。压完还超 1.6M 字符就再降一档质量
- **不做「这个模型支持看图」的开关**。一开始想加，但那样没勾的人会莫名其妙找不到图片按钮 —— 这个项目里「明明有这个功能却看不见」已经被吐槽过一次了。现在一律能加，模型不支持就让它报错，路径更短
- **点图能开大图**：渲染层的 CSP 是 `img-src 'self' data:`，`window.open` 会被拦，所以由主进程落一个临时文件、开一个只显示这张图的窗口，关掉时把文件删了
- 一条消息最多 6 张；发送后待发列表清空，不会粘到下一句

### 📝 顺手改了文档里的旧话

README 的「已知限制」原来写着「不能发图片给模型看（多模态），也不能让模型生图」。
前半句现在不成立了，改成了：看图**支持了，但模型本身得支持视觉**；生图还没做，
并且点明了**生图和聊天是两个模型**（这正是你之前问的那个问题）。

### 🧪 冒烟测试 202 → 216 条

界面侧 11 条（加图按钮 / 缩略图 / × 撤销 / **粘贴一张真的 File** / 发送后清空 /
气泡里出图 / 文字还在 / 只发图也能发），宿主侧 3 条：

- **发给模型的是多模态数组**（翻遍所有请求找数组形态的 user 消息）
- **数组里同时有文字段和图片段**（且图片是 data:image/ 开头）
- **没带图的消息仍然是纯文本**（防止把普通请求也改成数组）

> 关键在那个宿主侧断言：界面上有缩略图、气泡里也有图，看起来完全正常，
> 但如果发出去的还是字符串，模型只会当你没发图 —— 这种错界面上一眼看不出来。

---

## 2026-09 分支与存档点：走另一条剧情线而不丢掉原来那条

### 🎯 原计划是「消息从数组改成树」，改掉了

按最初的计划，⑤ 应该把 `convo.messages` 从数组改成**消息树**（酒馆那种做法）。
盘了一遍发现：**`convo.messages[index]` 在这个代码库里被用了 100 多处**（渲染、编辑、
删除、重新生成、状态面板、导出、记忆分段…），改成树等于把这些全重写一遍，
而且崩了是**丢对话**级别的后果。

换成两个**不动数据结构**的做法，功能上够用：

| | 怎么实现 | 好处 |
| --- | --- | --- |
| **分支** | 从某条消息**另开一个会话**，把前 N 条复制过去 | 复用现成的新建会话逻辑；**当前会话一个字节都不动** |
| **存档点** | `convo.checkpoints = [{ id, at, name, messages, panel, summaries, player }]` | `conversations.json` 没有白名单，加字段就落盘；读档 = 还原数组 |

理由：「走另一条剧情线」这个需求，**会话副本做得更好** —— 真·树结构在界面上其实很难
看明白（哪条是哪条），而两条会话并排一眼就懂，还能直接对比。代价是没有拓扑视图。

### 两件事的「代价」必须在界面上说清楚

- **分支**：什么都不丢
- **存档点**：读档会把对话**还原成存档时的样子**，存档之后聊的会丢

所以读档前会弹确认框，文案里直接写明「这段时间聊的内容会丢掉」，
并补一句「想保住现在这条线的话，用消息上的『分支』更稳」。用户不会以为读档也能反悔。

**分支不搬摘要**：摘要压缩的是「最早那批消息」，而新会话里这批消息是原样留着的，
搬过去等于同一段内容被记两遍 —— 新线从零开始攒记忆。

**存档点上限 12 个**：每个都是整段对话的深拷贝，攒多了会把 `conversations.json` 撑大。

### 🧪 冒烟测试 182 → 202 条

新增场景「会话：分支与存档点」：分支后多出一个会话且成为当前 / 标题标了「分支」/
前 N 条原样复制 / 内容对得上 / 绑定跟着走 / **原来那条线完好无损**（最关键的一条）/
存档点空状态 / 存一个档 / 档名写明条数 / 存完再聊会变长 / 读档前先确认 /
读档后退回存档时的条数 / 存档点本身还留着 / 删掉后回到空状态。

> 写这条时踩到一个**测试自己的坑**：落盘是防抖的（350ms），我等 300ms 就去读磁盘，
> 读到的是上一步的快照，于是「会话长长了」误报失败。改成同时断言界面条数和磁盘条数，
> 这样两边对不上也能立刻看出来。

---

## 2026-09 重新生成候选（swipe）：老的那条不扔了

### 🔁 「重新生成」以前是个破坏性操作

写了一大段挺满意的回复，只有一句不对 → 点「重新生成」→ **整条没了**，换成一个新的，
而且十有八九还不如原来那条。这是个很反直觉的设计：明明只是想「再抽一次」。

现在「重新生成」变成**加一条候选**：老的那条留着，角色名旁边出现 `‹ 2/3 ›`，
左右翻着挑，挑到满意为止。

### 实现上只动了一个地方

消息上多两个字段：

```js
{ content: '…', variants: ['老的那条', '新生成的那条'], variantIndex: 1 }
```

**`content` 永远等于当前显示的那条**（`variants[variantIndex]`）。不改这个约定是有意的：
历史构建、复制、导出、状态栏解析读的全是 `content`，所以那些地方**一行都不用动**。

跟着一起同步的地方有三处，不然会「切走再切回来变回老样子」：

- 「编辑」改的是当前这一条 → 写完也要落回它那个候选槽
- 「继续」是往当前这条后面续写 → 收尾时同步
- 生成失败/被停止 → 那个占位的空候选要撤掉，不然会留下一条空白候选

### 放在角色行，不放操作栏

一开始我把 `‹ 2/3 ›` 放进了消息操作栏，截图一看**根本看不见** —— 那个操作栏是
`opacity: 0` 悬停才浮出的。放那儿等于「有候选」这件事发现不了。

挪到角色行（和模型名同一行）之后就常驻可见了，一眼能看出这条留了几个版本。

### 🧪 冒烟测试 169 → 182 条

新增场景「消息：重新生成候选」：只有一个版本时没有切换条 / 重新生成后变 2/2 且正文是新的 /
左翻回 1/2 且正文回到老的 / **老的那条没丢** / 右翻回 2/2 / 落盘后
`content === variants[variantIndex]`。

顺带把假模型的回复改成带序号（`冒烟测试回复 #2：…`）—— 不然两次生成的内容一模一样，
根本测不出「现在显示的是哪一条」。

---

## 2026-09 世界书递归扫描（设定能自己串起来）

### 🔗 只提一个词，把整条知识链铺开

**以前**：一本书越写越厚之后，你得**主动提到**某个词那条设定才会进来。
你写了「十二泰坦」的详细设定，但正文里只说了「翁法罗斯」—— AI 就完全不知道十二泰坦是什么。

**现在**：条目里多了一个 **「递归」** 勾。勾上的条目命中之后，**它的正文也会被拿去再匹配一遍**，
于是正文里提到的词能触发别的条目，一路连锁：

```
「翁法罗斯是个什么样的地方？」
  → 【世界总览】命中
    → 正文里有「十二泰坦」      ← 递归再扫
      → 【十二泰坦】命中，正文里有「火种」
        → 【火种】命中……
```

### 两道闸，防止烧 token

递归的代价很直接：每多一层就多塞几条设定。所以：

| 闸 | 在哪 | 默认 |
| --- | --- | --- |
| 哪些条目往下带 | 条目表单里的「递归」勾 | **不勾**（只给总览 / 索引这类开） |
| 最多连锁几层 | 设置 → 行为 → 世界书递归深度 | `3`（`0` = 完全关掉） |

「预览命中」的提示会告诉你这次带出了几条、**其中几条是递归来的** —— 不然用户只会觉得
「怎么突然多塞了这么多设定」。

导出的世界书同时写 `recursive`（我们认）和 `excludeRecursion`（酒馆认，语义相反），
所以导出的书酒馆能用，我们自己再导回来也不丢这个开关。

### 🧩 匹配引擎抽成了 `main/worldbook-match.js`

关键词判定、副关键词逻辑、概率、递归扫描这些纯逻辑全都搬了过去，这样冒烟测试能
**require 同一份实现** —— 假后端的 `worldbooks:preview` 直接调它，递归跑的是真代码。

顺带把「命中判定」这块从主进程里摘干净了：`main.js` 从 1810 行降到 1520 行。

> 摘的时候踩了个坑：`worldbookEntriesByIds` 夹在 `entryMatches` 和 `matchWorldbookEntries`
> 中间，被一起切走了。`node --check` 过不了才发现的（语法没错，是运行时才会炸）。
> 后来写了个脚本**逐个顶层定义和 HEAD 对比**，确认「少了的 14 个全都有意搬进了新模块」，
> 才敢继续。**大块搬迁别靠肉眼核对行号。**

### 🧪 冒烟测试 158 → 169 条

- 界面层：会话里出现「翁法罗斯」→ 点「预览命中」→ 确实带出了十二泰坦和火种、
  提示里写明「2 条是递归带进来的」、没命中的条目不会被塞进来
- 引擎层（宿主侧直接打模块）：深度 0 只给直接命中的那条 / 深度 1 带出第一层 /
  深度 3 逐层接力到没得带（3 条 3 轮）/ 命中过的不会重复 / **没勾递归的条目不会往下带**

> 写这组测试时我自己先错了：最初的「链」其实是**扇出**（总览正文里同时写了
> 「十二泰坦」和「火种」，所以深度 1 全出来了）。改成严格链式才验得出「逐层接力」。

---

## 2026-09 导出：角色卡 / 世界书 / 对话

### 📤 以前只能导入，不能导出

三处入口：

| 在哪 | 导出成 | 说明 |
| --- | --- | --- |
| 角色编辑器右下角「导出」 | **PNG 角色卡** 或 **JSON** | 一个按钮两种格式，**由你在保存框里选的后缀决定** |
| 世界书编辑器「导出」 | lorebook JSON | 酒馆能直接导入 |
| 聊天右上角「导出」 | Markdown | 会话存档；顺带把状态面板和角色/世界信息写在开头 |

**PNG 卡是真卡**：不是把 JSON 塞进文件名，而是把卡数据 base64 之后写进 PNG 的
`tEXt` 块（关键字 `chara`），和酒馆的格式一模一样 —— 导出的图既能看，又能被酒馆
或本程序重新导入。没有头像时用 canvas 画一张带首字母的占位图。

**自家字段也能带走**：年龄 / 性别 / 种族 / 属性 放在 `extensions.barbara` 里 ——
规范里 `extensions` 就是给各家放私有数据的地方，酒馆会原样保留，我们自己也能读回来，
所以「导出 → 再导入」是一个闭环。

### 🧩 顺手把 PNG 的读写抽成了 `main/png.js`

读（解析导入的卡）和写（生成导出的卡）现在在同一个模块里，**冒烟测试能 require 到同一份实现**，
于是「导出的卡到底能不能被导入」是真跑过的，而不是靠人眼对比。

test 里那条断言就是完整往返：
```
渲染层生成的 PNG 字节 → pngWithTextChunk 插 tEXt → parseCharacterCardPng 读回来 → 核对字段
```

### 🐛 踩到的一个坑（记下来）

给「导出结果」起的变量名一开始叫 `exports` —— 而 CommonJS 模块自带一个 `exports` 参数，
`const exports = []` 直接让 CJS 解析失败，Node 就改按 **ES module** 解析，于是
`require is not defined` 就炸了。**别用 `exports` / `module` / `require` 当变量名。**

### 🧪 冒烟测试 150 → 158 条

新增场景「导出」：三个入口点得通、有提示、空会话不给导出，以及宿主侧四条
（PNG 真往返、文件名默认、世界书是 lorebook 形状、会话是能读的 Markdown）。

---

## 2026-09 消息可以直接改，回复没写完可以「继续」

### ✏️ 就地编辑消息

以前改 AI 写错的一个字，只能「删除 → 重发」，而重发会换一整条**新的**回复 ——
写了一堆满意的内容，为了改个错字全丢了。

现在鼠标悬停在消息上就有 **「编辑」**：气泡变成输入框，改完 **Ctrl + Enter 保存**
（**Esc** 取消）。

- 编辑框里给的是**原文**（带 `**` `==` 这些标记），不是屏幕上渲染后的文本 ——
  给渲染后的文本一保存，标记就全没了
- 内容是空的会被拦下来（想删就用「删除」）
- 改的是 AI 的回复时，会**重新扫一遍状态面板**（你可能正好把状态栏那几行改掉了）

### ➡️ 继续生成

回复被 maxTokens 截断时，鼠标悬停在**最后一条** AI 回复上点「继续」，它接着往下写。

实现上有个省事的地方：**不新建消息**。因为流式分片本来就是
「把增量加到 `messages` 里最后一条上、再画到它的节点上」，所以只要不加新消息，
它自然就续写在原文后面了。另外补了一种情况：有的服务商不推流式分片、直接给全文，
那种时候分片处理器一次都没跑过，得手动追加一次。

「继续」**只出现在最后一条** —— 中间的回复后面早就接上别的话了，续写没有意义。

### 🐛 顺手修的：编辑框把按钮顶出视口

截图时发现的：编辑框比原来的气泡高，展开后「保存 / 取消」正好被切在消息区下边缘外面。

第一次修还修错了 —— 我 `scrollIntoView` 的是 textarea，而**它自己已经完整可见**，
按规范就该什么都不做；被切掉的其实是它下面那行按钮。改成滚整个内容块之后才对
（量出来正好要滚 22px）。

> 顺带一条环境经验：**别用 `requestAnimationFrame` 包这类"要能被观察到"的动作** ——
> 隐藏窗口根本不产生动画帧，包了就永远不执行。`scrollIntoView` 自己会强制布局，直接调就行。

### 🧪 冒烟测试 135 → 150 条

新增场景「消息：编辑与继续」：继续只出现在最后一条 / 继续是追加不是替换 /
编辑框给的是源文本 / 取消不生效 / 空内容被拦 / 保存后重新渲染 / 落盘 /
**「保存 / 取消」在视口里**（就是上面那个 bug 的复发检测点）。

顺带把假模型的**流式分片改成拼起来等于返回值** —— 之前分片只是返回值的前缀，
那会让「继续」这种依赖分片的路径验不准。

---

## 2026-09 抽了层建 DOM 的小工具（`ui/build.js`）

### 🧱 不是引组件库，是把 `createElement` 样板收掉

之前讨论过「要不要引组件库」。结论是**不引框架**：界面就这些（9 个弹窗 + 3 个页面），
而引一套组件库要付出构建步骤的代价，但「零构建」是这个项目写在 README 里的卖点。
真正缺的不是组件库，是**把重复的 DOM 拼装收掉** —— 渲染层原来有 **94 处 `createElement`**，
光是「建一个按钮」那个五连击就重复了 20 遍。

新增 `renderer/js/ui/build.js`（不到 120 行，零依赖）：

| 工具 | 作用 |
| --- | --- |
| `h(tag, props, ...children)` | 建元素。`class` 支持数组（假的滤掉，方便条件类名）、`text`、`dataset`、`on*` 事件 |
| `button({ text, onClick, ... })` | 干掉那 20 遍五连击 |
| `card({ ... })` | 列表页卡片外壳（头像 / 名字 / 小字 / 操作区） |
| `renderListPage({ ... })` | 列表页骨架：清空 → 空状态 → 副标题 → 铺卡片 |
| `append` / `clear` | 小杂活 |

**两条硬约定**（写在文件头）：

- **不提供 `innerHTML` 入口** —— 渲染层里很多内容来自模型，绝不能让它们有机会变成 HTML
- `children` 里的 `null` / `false` 直接跳过，这样能写 `cond && h(...)` 这种条件渲染

### 📉 效果

先拿最值得改的几处开刀（都在这套冒烟测试的覆盖范围内）：

- 角色列表页 / 世界书列表页 —— 原来**结构相似度 100%**，现在共用 `renderListPage`
- `characterCard` / `worldbookCard` / 本书角色的 chip 条 —— 共用 `card()` + `button()`
- 会话列表、状态面板行、世界书条目列表、属性编辑行

```
document.createElement：94 → 52 处
main.js 行数：        5159 → 4778 行（-381）
冒烟测试：            135 条全过，行为零变化（改完两页截图逐项核对过）
```

剩下的 52 处大多是**只出现一两次**的（`option` / `optgroup` / `details` / `canvas`），
继续换收益递减；新写的代码直接用这套工具就行。

---

## 2026-09 右上角「外观」：字号 / 加粗颜色 / 背景图

### 🎨 对话窗口能调了

顶栏加了个 **「外观」**（在「视角」旁边），三样东西，**改完立即生效，不用点保存**：

| 设置 | 说明 |
| --- | --- |
| **正文字号** | 12–22px 滑块。标题、表格、心理/旁白**跟着一起缩放** —— 里面原本写死的 15px / 13px / 12.5px 都换成了 em，不然把正文调到 22px 时标题还是死的 15px，看着就不对了 |
| **加粗字的颜色** | 原生**色盘** + 手填十六进制（`#e06c75`，不带 `#` 也认，`#abc` / `#rrggbbaa` 都收） |
| **对话窗口背景图** | 铺在消息区当壁纸，走的是和头像同一条选图链路（主进程弹文件框 → canvas 压缩 → 存 dataURL） |

几个设计决定：

- **`==高亮==` 不受加粗颜色影响** —— 它有自己的一套（主题色 + 底色）。两档可以配着用：
  加粗设成暖色、高亮保持蓝色，一眼就分得出轻重。
- **背景图只等比缩**（头像那套是居中裁正方形）—— 背景裁了就变形了。压到最长边 1920、webp q0.82 再存。
- **颜色只收 `#rgb` / `#rrggbb` / `#rrggbbaa`**，正则卡死。不然有人往设置文件里塞
  `red; background:url(...)` 这种东西，就顺着 CSS 变量注进去了。
- **背景图有大小上限**（4M 字符）。config.json 每次改设置都要整份重写，不能让它被一张图撑爆。
- 字号**拖动时实时预览，松手才落盘** —— 否则拖一次要写几十遍配置文件。
- **滑块轨道是自己画的**：Chromium 默认那条「还没选到」的轨道，浅色下接近**纯黑**、
  夜间又偏亮，跟这个面板的配色完全对不上。现在已选部分用主题色、未选部分用和开关
  同一条 `--toggle-track`，比例靠 `--range-fill` 跟着值走。

### 🧪 冒烟测试 120 → 135 条

新增场景「对话窗口外观」，验的是**真效果**而不是"设置存下来了"：

- 字号：`getComputedStyle(气泡).fontSize` 真的从 14px 变成 20px
- 颜色：`getComputedStyle(加粗).color` 真的是 `rgb(224, 108, 117)`；不带 `#` 也认；
  **乱填（`red`）被拒绝且不覆盖原值**
- 背景图：假后端返回一张**真的 1×1 PNG**，所以「解码 → canvas 缩放 → 存 dataURL」
  这条链路走的是真代码；断言 `#messages` 的 `background-image` 真的挂上了、清除后真的没了

---

## 2026-09 重点标出来：加粗 + 高亮

### 🖍 长段落看得累，所以给了两档重点样式

| 写法 | 效果 |
| --- | --- |
| `**文字**` | **加粗** |
| `==文字==` | **加粗 + 变蓝 + 一点底色** |

**加粗本来就能渲染**（`renderInline` 里早就有 `**` 规则）—— 真正的问题是
**没人告诉 AI 去用**，所以它从来不写，结果就是一大段平铺直叙。
这次补的是三件事：

1. **新加 `==高亮==`**（加粗 + 主题色 + 淡底色）。选 `==` 是因为它是 Markdown 高亮的
   通行写法（Obsidian / Typora 都认），模型更容易照做。
2. **明确告诉模型怎么用**：扮演规则和主持规则里都加了一段「标重点」——
   关键信息（数值、名字、时间地点、重要决定）用 `**`；
   真正不能错过的（危险、转折、关键线索、承诺）用 `==`；
   并且**一篇里标一两处就够，整段都标等于没标**。
3. **样式跟着主题走**（用 `--accent` / `--accent-soft`），浅色和夜间两套都验过截图。

### 📝 你自己写的内容也认

世界书的「开场白」、角色卡的「开场白」都会原样渲染 ——
想在一大段开场里点出重点，直接写 `==天黑了，别出门。==` 就行。

### 🧪 冒烟测试 117 → 120 条

假模型的回复改成带两种标记（`**这是加粗**，==这是高亮==`），断言：
渲染成真的 `<strong>` / `.msg-em` 元素，而且**标记符号本身没有露出来**。

---

## 2026-09 状态面板默认收起，顶栏的「状态」按钮去掉

### 📐 默认收起

以前切到有面板的会话时是**自动展开**的，一屏最多被吃掉 30vh；
而且面板「从无到有」时还会自己弹开一次。现在**一律默认收起**：

- 切换会话 → 回到收起状态（只留 50px 的一条细条）
- 同一个会话里你手动展开的状态会保留（不会在你读的时候被打断）

细条本身就带着开关，所以「默认收起」不等于「找不回来」：

```
› 当前状态  8/8 项已填                    [重置]
```

### 🧹 顶栏的「状态」按钮去掉了

顶栏原本还有个 `状态` 按钮（只在有面板时才出现），和面板自己那条开关是同一个功能，
纯属重复。而且它长得像个标签、不像开关 —— 这也是当初「看不到收起」的原因之一。
现在只有一个开关，就在面板标题上。

顶栏剩：`模型下拉 · 视角 · 记忆 · 复制全文 · 清空对话`。

### 🧪 冒烟测试 114 → 117 条

「状态面板」场景改成断言**默认就是收起的**：默认收起、字段区不显示、面板仍在（细条）、
开关可见可点、点一下展开（且比收起高）、再点收起、点标题栏空白处也能切换，
外加一条「顶栏的『状态』按钮已移除」，防止它被顺手加回来。

---

## 2026-09 修复：单角色对话时 AI 不知道角色的年龄

### 🐛 16 岁的角色被回复成 21 岁

**症状**：绑一张角色卡单独聊天（角色库 → 聊天），卡上写了「年龄 16」，
AI 却在回复里写成 21 岁 —— 它根本不知道年龄。

**原因**：加「年龄 / 性别 / 种族」三项时，我只把身份接在了**游玩世界书**那条路径上
（`startWorldPlay` → `seedIdentity`）。单角色对话走的是 `applyCharacterChoice`，
那条路只种了卡上的「属性」，**没种身份**；而且这三个字段也没进角色信息块，
所以对模型完全不可见 —— 它只能自己编。

**修复**（两处，互为兜底）：

1. **角色信息块里加上身份**（`buildApiMessages`）：
   ```
   【昔涟的基本信息】
   年龄 16，性别 女，种族 人类
   ```
   这是最可靠的一条 —— 不管面板在不在、有没有被「重置」，模型一开始就知道。
2. **单角色对话也把身份四项种进状态面板**（`applyCharacterChoice` → `seedIdentity`）。
   顺带把 `seedPlayerIdentity` 改名为 `seedIdentity`：同一个函数，
   两种场景的区别只是「这是谁的身份」（单角色对话是角色的，世界模式是你自己的）。

**顺带**：NPC 的身份也进了「这个世界的人」名单：
`- 艾莉（16岁·女·精灵）：……` —— 不然 GM 同样会瞎编 NPC 的年龄。

### 🧪 冒烟测试 110 → 114 条

- 属性场景补了三条：单角色对话的身份四项在面板里、值来自角色卡、姓名取角色名
- 宿主侧补了一条：真正发给模型的请求里含 `【属性测试角色的基本信息】…年龄 18`

> 阴性对照：把角色信息块里那行注掉，宿主断言立刻失败 —— 而它正是这个 bug 的复发检测点。

---

## 2026-09 修复：角色「属性」存盘时被静默丢掉（严重）

### 🐛 一个我自己引入的数据丢失 bug

**症状**：在角色编辑器里填好「属性（状态面板）」，点保存，**重启之后全没了**。

**原因**：主进程的 `normalizeCharacter()` 是**白名单式**的 —— 它只保留显式列出来的字段。
加「属性」功能时我忘了把 `attributes` 加进这个白名单：

```js
return {
  id, name, avatar, ..., creatorNotes,
  age, gender, race,      // ← 这三个加了
  tags, source, createdAt, updatedAt
  // attributes 不在里面 → 存盘时被剥掉、读盘时也被剥掉
};
```

而 `loadCharacters()` 和 `saveCharacters()` **都**会过这个函数，
所以那批属性从来没被写进过 `characters.json`。

**修复**：把归一化搬到 `main/characters.js` 独立成模块（顺便让测试能 require 到**同一份真代码**），
并在白名单里补上 `attributes`（去重、限 24 字名字 / 500 字值 / 120 条）。

**为什么会漏掉测试**：冒烟测试当时用的是自己写的假后端，**只做存取、不做归一化** ——
主进程白名单漏字段这种事，它根本测不出来，所以一路全绿。
现在假后端改成调用真正的 `normalizeCharacter`，并新增场景「角色卡：字段往返不丢」：
把**每个可编辑字段**都填上不同的值，存完逐个核对回来没有。

> 阴性对照：把白名单里 `attributes` 那行注掉，测试立刻报 4 条失败；
> 而在修好测试之前，同样这份坏代码会 100% 全绿。

### ✨ 属性支持粘贴文本，批量生成

点属性区的 **「粘贴文本」**，把一段文本贴进去 →「解析并加入」，一次生成多条。

一行一项，认这几种写法：`金币：9900`、`【金币】：9900`、`金币 9900`、`- 金币：9900`。
**同名覆盖值**（不会加出第二条），认不出的行自动跳过，保留字不收。
结果会提示 `已解析：新增 5 项，更新 1 项，跳过 2 行`。

> 这个功能正好也能用来补数据：从世界面板里把字段抄出来一贴，角色卡就补回来了。

### 🧪 冒烟测试 86 → 110 条

新增两个场景：「角色卡：字段往返不丢」（14 条字段核对）、
「角色属性：粘贴文本批量生成」（10 条：四种写法、跳过规则、保留字、同名覆盖、落盘往返）。

---

## 2026-09 状态面板可折叠、角色卡加身份三项

### 📐 状态面板：随时收起，随时展开

之前面板只有一个**22×22、颜色很淡的 `×`** 在右上角，而且 `×` 在习惯上意味着「关闭」——
结果就是「收起」这个功能实际上没人看得见，面板一直占着 146px 挡着看故事。

现在：

- 左边是 **`︿ 当前状态`**，箭头方向就是状态（`︿` 展开着 / `›` 收起了）
- **整条标题栏都能点**，不用瞄准那个小按钮
- **收起后不消失，只留一条细条**（50px）：
  `› 当前状态  8/8 项已填` —— 随时有地方点开，不会「收起来就找不回来了」
- 顶栏那个「状态」按钮保留，是同一个开关

消息区高度：收起后从 396px 涨到 492px。

### 🪪 角色卡新增：年龄 / 性别 / 种族

身份三项，挨着「角色名」放一行：

- **年龄**、**种族** 是输入框（「不详」「精灵」这种都得能写），**种族默认「人类」**
- **性别** 是下拉框：未设置 / 男 / 女 / 其他
- 导入酒馆卡时会归一化：卡里写 `female` / `male` / `other` 也能认出来，
  认不出的一律留空 —— 宁可不填，也不要塞一个编辑器里选不中的值进去（那样一保存就丢了）
- 酒馆卡规范里本来没有这三个字段，所以导入的卡大多是空的，编辑器里补就行

### 🔄 身份四项会进状态面板（玩世界书时）

**姓名 / 年龄 / 性别 / 种族** 会自动作为字段进面板 —— 因为世界里时间会走、剧情会推：
过一年年龄要涨一岁，被人改名也得跟着改。交给「每轮由程序权威注入」的面板维护，
比指望模型自己记住靠谱。

- **姓名**用「游玩」弹窗里填的那个（才是 GM 实际称呼你的名字），其余三项来自角色卡
- 没选角色卡就只有姓名 —— 手填的玩家没有年龄/性别/种族
- **剧情改了面板里的「姓名」，消息上的名字标签和 `{{user}}` 会跟着变**
  （`syncPlayerNameFromPanel`：不跟着改就会出现「面板说你叫 A，界面还叫你 B」的矛盾）
- **NPC 的身份不进面板**：一局有好几个 NPC，都塞进去会出现「五个【姓名】」互相覆盖。
  NPC 的信息在「这个世界的人」名单里，那部分本来就每轮都告诉 GM

### 🧪 冒烟测试 72 → 86 条

- 新增场景「状态面板：随时收起 / 展开」：收起后字段区高度为 0、
  面板**仍在且变矮**（不是消失）、开关**仍然可见可点**、再点能展开、
  点标题栏空白处也能切换。「收起后还能不能打开」是这次的核心，所以专门钉住。
- 属性场景补了身份三项的落盘断言；进入世界场景补了「身份四项在面板里且值正确」，
  并多发一条消息，让宿主侧能验证 `【姓名】：改过的名字` / `【年龄】：18` 真的注入给了模型。

---

## 2026-09 进世界时可以直接用角色库的人物当自己

### 🎭 不用再手打一遍设定了

「游玩」世界书时会先弹一个「创建你的角色」—— 以前只能手填名字和设定。
现在最上面多了个下拉框，**可以直接选一张角色卡当自己**：

- 选完**名字和设定自动填到下面**，而且是「填」不是「锁」—— 填完还能改，改了以你改的为准
- 下面会显示一行预览：**这张卡会带上哪些属性**（没定义属性也会说清楚）
- 这张卡的**「属性」会一起种进状态面板** —— 接上一轮那个功能：
  之前世界模式里玩家角色只有名字+简介，角色卡的属性用不上；现在闭环了
- 角色库为空时整块自动隐藏，什么都不影响

**同名属性的优先级**：先种「你自己」那张卡的，再种本书角色的（同名以先出现的为准）。
所以自己卡上的「金币」不会被书里的同名属性盖掉 —— 主角的数值优先。

### 🧩 实现上没动的几处

- **不绑角色卡**：玩家的身份存在 `convo.player`（新增 `characterId` 记住选的哪张），
  `convo.characterId` 仍然是 null。绑上去会让 AI 去扮演你，那就反了
- **不用角色卡的开场白**：进哪个世界就用那个世界的开场
- **不改消息气泡头像**：你的消息还是显示「我」。要给玩家也显示头像的话是另一件事

### 🧪 冒烟测试 60 → 72 条

「进入世界」场景重写成完整链路：下拉里列出角色库的人 → 选一张 →
名字/设定自动填上 → 预览说明带哪些属性 → 改个名字（验证「还能改」）→ 开始游玩 →
面板里出现这张卡的属性且值正确 → 会话里记下了 `player.name` / `characterId`。

宿主侧断言也补了一条：**真正发给模型的 messages 里包含「【玩家角色：改过的名字】」**。

> 顺带修了宿主侧断言的一个隐患：原来只看「最后一次请求」，但世界没写开场白时
> 进世界会立刻多发一次「生成开局」的请求（那次不带状态面板），只看最后一条会误判。
> 现在把每次请求都收进来，按内容找。

---

## 2026-09 角色卡上能看见「标签」了

### 🏷 标签 = 分类，现在真的看得见

角色编辑器里的 **「标签」** 一栏是从酒馆（SillyTavern）角色卡规范搬过来的 `tags` 字段，
用途是**分类**：这张卡属于什么类型（作品、风格、用途）。
示例卡里就是这么填的：`崩坏星穹铁道 / 昔涟 / Cyrene / 翁法罗斯 / 官方设定 / 安静 / 治愈`。

**但之前它是个死字段** —— 全项目只有 5 处碰过它（导入时写入、打开编辑器时填入、
保存时写回、DOM 绑定、新建时给空数组），**没有第 6 处**：
不进提示词、不参与匹配、不显示在任何地方。也就是说，在编辑弹窗之外你根本看不到它，
填了等于没填。

现在把它显示在**角色卡的那行小字**上：

```
手写 · 原神 · 治愈
```

- 塞进已有的「来源」那一行，**卡片高度不变**，不会让卡片参差不齐
- 最多显示前 3 个，超出用省略号（`title` 里有完整内容）
- 编辑框的占位符也改成「分类用，例如：原神, 治愈」，把用途说清楚

> 暂不做搜索/筛选 —— 那是角色攒到几十张以上才有意义的东西。
> 数据一直都在（`characters.json` 里的 `tags` 数组），要加随时能加。

### 🧪 冒烟测试 58 → 60 条

在「新建角色」场景里补了两条：卡片上能看到分类标签、来源标记没被挤掉。

---

## 2026-09 角色「属性」：开局就让 AI 知道要维护哪些数值

### 🎒 从「等 AI 自己冒出来」到「你先声明好」

状态面板本来就有（AI 回复里出现 `【金币】：100` 就被自动收进面板，每轮权威注入，
数值不会漂），但它有个**冷启动问题**：字段全靠模型自己碰巧输出，
第一轮你不知道它会维护什么，同一个角色开新局还得重新被发现一遍。

现在可以在**角色编辑器**里先把属性声明好：

- **「属性（状态面板）」** 一栏：手写属性名，或者点下面的**快捷候选词**加进来
- 每个属性配一个**初始值输入框**（不是纯文本，是输入框）
- 绑上这个角色（或进入放了这个角色的世界书）时，属性自动**种进状态面板**
- 并且**第一轮就把字段名告诉 AI**，让它按 `【字段】：值` 的格式维护

> **为什么不做成「文本里写 [金币]：100」？** 因为那样每次改数值都得把文本复制出来、
> 改完再粘回去。现在从编辑角色到游玩，全程都是输入框。

### ⚙️ 快捷候选词放在「设置 → 状态属性」

就是一串名字（逗号或换行分隔，存 `config.json`），在角色编辑器里点一下就加一个属性。

**刻意不做的事**：没有插件系统、没有 per-角色 的自定义清单、没有字段类型系统。
理由是：它只是「一堆常用名字」，不配拥有自己的定义/存储/管理界面。
真到了需要给属性附加额外信息的时候（类型、默认值、是否常驻、显示顺序），
那才值得升级成独立栏目 —— 那时候叫「属性模板」才名副其实。

### 🧩 几个设计决定

- **初始值 vs 当前值分开存**：角色卡上是模板，会话面板里是这一局的当前值。
  改角色卡不会动到正在进行的游戏。
- **同名不覆盖**：半路给会话绑角色，只补面板里还没有的字段，已有的当前值保留。
- **保留字拦得住**：心理 / 旁白 / 扮演规则 这些是提示词自己的段落标记，当属性名会打架，
  添加时会提示换一个。
- **面板改成「累积」而不是「从历史重建」**（`syncConvoPanel`）：
  原实现每轮都用历史重建字段表，模型这一轮没提到的字段会连值一起被抹掉 ——
  角色卡种下的字段和用户手动加的字段都会中招。现在以现有面板为底、把历史里扫到的值盖上去。

### 🧪 冒烟测试 46 → 58 条

新增场景「属性：从角色卡种到状态面板」，覆盖完整链路：
设置里改候选词 → 落盘 → 快捷按钮出现 → 点击加属性 → 填初始值 → 保存落盘到角色卡 →
点「聊天」绑定 → 面板里出现字段且值正确。

另外补了一条**宿主侧断言**：检查 `chat:send` 真正发出去的 messages 里
包含 `[当前状态]` 和 `【金币】：100` —— 页面上看不出模型收到了什么，
而这条链路正是这个功能的全部意义。（把 `seedPanelFromCharacters` 注掉做阴性对照，
面板断言和注入断言会同时失败。）

---

## 2026-09 角色卡上可以直接删除

### 🗑 不用再「点进编辑」才能删角色了

以前删一个角色要绕一大圈：**角色库 → 点「编辑」→ 弹窗左下角「删除角色」→ 确认**。
为了删一样东西先打开一个表单，说不过去。

现在角色卡**右上角有一个 `×`**，平时透明，**鼠标移到卡片上才浮出来**：

- 点它直接弹确认框（文案和以前一样，会说明「用到它的会话会变回通用助手」），确认即删。
- 静止时零视觉噪音 —— 卡片平时还是干净的「编辑 / 聊天」两个按钮，
  破坏性操作不跟「聊天」挤在一起，降低误点。
- 键盘 Tab 到它时也会显形（`:focus-visible`），不是只有鼠标能用。
- **弹窗底部的「删除角色」保留** —— 正在编辑的时候用那个更顺手。

交互直接沿用左侧会话列表那套 `×`（悬停浮现、悬停变红），学一次就够。

### 顺手把删除逻辑拆开了

`deleteCharacter()` 原来是**和编辑器绑死**的：它读「当前正在编辑谁」，
删完还负责关弹窗 —— 所以卡片上根本没法复用。

现在拆成 `deleteCharacterById(id, scope)`：

- `scope = 'library'` 从角色库删（卡片走这条）
- `scope = 'worldbook'` 只从当前这本书里移除副本（编辑器走这条）

「解绑会话」「落盘」「刷新界面」这些只有一份实现，两个入口行为不会跑偏。

### 🧪 冒烟测试补了 9 条断言（37 → 46）

新增场景「角色库：卡片上删除」，覆盖：取消时角色还在、确认后卡片消失并落盘、删空后回到空状态。

另外补了一条**只在宿主侧能做的断言**：*鼠标移上去删除按钮才会浮出来*。
`:hover` 只认真实指针，页面里派发 `mouseover` 不算数；隐藏窗口里 `sendInputEvent`
也不会触发 hover。最后用 CDP 的 `CSS.forcePseudoState` 强制加 `:hover` 来验真实的选择器匹配
（顺带发现：隐藏窗口不产生动画帧，**带 `transition` 的 opacity 永远不会推进**，
所以断言里要先临时关掉过渡）。

这条断言是有价值的：把选择器故意写成 `.char-card :hover`（多一个空格）后，
测试立刻报 `✗ 鼠标移上去才会浮出来 ← opacity=0` 并以退出码 1 结束。

---

## 2026-09 界面代码拆成 ES module（依然是零构建）

### 🧱 `renderer.js`（4939 行）拆开了

一个文件装 185 个函数已经到极限，现在按**功能**拆成模块。
**没有引入任何打包器** —— 用的是浏览器原生 ES module，`<script type="module">` 直接加载：

```
renderer/js/
├── main.js          入口（还装着大部分功能，会继续往 views/ 搬）
├── core/   config · state · dom（那 150 个 DOM 引用）· api（preload 桥）· util
├── ui/     toast（提示条）· confirm（确认框）· theme（日夜模式）· markdown
└── data/   persist（持久化）
```

- `state` 和 `el` 都归到了 `core/`，谁都可以 import。
- 几个「全项目都在用」的公共函数**原来住错了地方**，已经归位：
  `showToast` 之前住在「角色（角色扮演）」分区里（16 个分区都在调它），
  `confirmDialog` / `persistConversations` 住在「白天/夜间模式」分区里。
- 已实测：**当前 Electron 版本 + 当前那条 CSP** 下，`file://` 加载 ES module 完全正常
  （`import.meta`、preload 桥的 22 个方法都可用，控制台零报错）。
  所以 `npm start` 照旧，也不需要 webpack / vite。
- `npm run dev` 的「保存即刷新」不受影响 —— 它监听的是整个 `renderer/` 目录（递归）。

### ✅ 顺手补上了冒烟测试

```bash
npm run smoke
```

项目之前**没有任何测试**。现在有了：开一个不显示的窗口加载真实界面，
把所有 IPC 换成内存里的假后端（**不会碰你的角色和聊天记录**），
用「点真实按钮 + 读真实 DOM」的方式跑 **10 个场景 / 37 条断言**，
覆盖启动、主题、设置、角色库（新建/放弃/编辑）、聊天流式回复、世界书条目、
本书角色、玩家角色弹窗。

> 加新功能时顺手在 `tools/smoke-renderer.js` 里补一条场景，比每次手点一遍可靠。

### 当时还没做完的部分（现已完成）

写下这段时 `renderer/js/main.js` 还有 7900+ 行，功能要一块块搬进 `views/`。
**这部分后来已经全部做完** —— `main.js` 收敛到 482 行，只剩入口层编排，
`renderAll` 也按计划换成了刷新总线。想复核分层与规模：`node tools/analyze.js`。

---

## 2026-09 新建角色改成「保存后才生成」

### ✨ 改动：点「＋ 新建角色」不再直接建出一个空角色

以前点角色列表页的「＋ 新建角色」，**角色会立刻出现在列表里**（名字先叫「新角色」）——
而编辑器压根没打开，你得再点这张卡上的「编辑」才能开始写。

现在改成**草稿**：

- 点「＋ 新建角色」→ 打开一张空白的角色编辑器（标题是「新建角色」，副标题和底部提示都会
  写明「还没保存」），角色列表里**不会**多出任何卡片，磁盘上也不会有这个角色。
- 点「保存角色」的那一下，草稿才真正进角色库并落盘，提示变成「角色「XX」已创建」。
- 名字空着点保存会被拦下来，不会先建一个「未命名角色」再让你改。
- 没保存就关掉弹窗（✕ / Esc / 点弹窗外）会先问一句「放弃新建？」——
  确认放弃就等于没建过，**不会留下任何垃圾角色**；点取消则接着写。
- 草稿期间底部的「删除角色」是禁用的 —— 还没保存的东西没什么可删。
- 草稿状态下也能正常传头像（头像入口原来只认角色库里的角色，新建的会静默失效）。

**世界书 →「本书角色」→「＋ 新建」**同样处理：以前点一下就在书里挂一个空的「新角色」，
现在也是保存之后才加进这本书。

### 🐛 顺手修掉的两个问题

- **角色编辑器会被世界书弹窗盖住。** 所有弹窗的 `z-index` 都是 50，同层时谁在 DOM 里靠后
  谁在上面，而角色编辑器排在「世界书」弹窗**前面** —— 从世界书里点「编辑 / ＋ 新建」时，
  编辑表单其实被压在下面（只看得见一层模糊的背景），点了跟没反应一样。
  现在 `#chars-modal` 单独一层（60），确认框再高一层（70）。
- **作用域残留。** 在世界书里编辑过角色副本之后，`charEditorScope` 会一直停在 `'worldbook'`，
  这时回角色库点「＋ 新建角色」，新建出来的是**那本书里的副本**，角色库里反而什么都没多。
  现在这个按钮会把作用域钉死回角色库。

### 实现方式

新建出来的角色先只放在内存里的一个草稿 `charDraft = { character, scope, bookId }`，
它不属于任何列表：

- `editorCharacterById()` 优先认这个草稿，所以表单读写、传头像照常走；
- `editorCharacterList()` 看不到它，所以角色列表页和世界书那一排都不会画出来；
- 只有 `saveCharacter()` 里调用的 `commitCharDraft()` 会把它 push 进 `state.characters`
  或那本书的 `characters`，之后才 `persistCharacters()` 落盘；
- `closeCharsModal()` → `discardCharDraft()`：直接忘掉 —— 它从来没进过任何列表，
  连界面都不用重绘。

---

## 2025-12 角色可以自己上传头像了

### 🖼 新增：角色头像上传

之前头像只能来自导入的酒馆角色卡，**手写的角色是设不了头像的**
（`#char-avatar` 只是个显示用的 div，没有任何入口）。现在补上了：

- 角色编辑区左上角的头像方框**变成了按钮**，点一下弹系统文件框，
  鼠标移上去会浮出「换头像」提示。
- 旁边多了一个「清除头像」按钮，**只在有头像时才出现**。
- 支持 PNG / JPG / WebP / GIF / BMP，单个文件上限 12MB（和角色卡一致）。

**实现方式**（没有服务器，靠主进程当本地后端）：

```
渲染进程                        主进程（Node.js）
──────────                     ─────────────────
点头像       ──IPC────▶   dialog.showOpenDialog   弹文件框
                          fs.readFileSync         读文件
                          按扩展名判断 MIME
             ◀─dataURL──
canvas 压成 256×256
<img src="data:..."> 显示
```

页面被 CSP（`default-src 'none'`）挡着读不了文件，所以必须由主进程代劳 ——
这条链路和导入角色卡完全一样。CSP 里 `img-src` 本来就放行了 `data:`，
所以不用改任何安全策略。

### 🗜 头像会自动压到 256×256

新增 `shrinkAvatar()`，用 **canvas 标准 API**（不引入任何依赖）把选中的图
居中裁成正方形再缩到 256×256，导出成 WebP（支持透明，体积小）。

原因：头像显示出来最大也就 86px，一张手机照片直接 base64 存进
`characters.json` 会让这个文件涨到几十 MB，而**每次保存设置都要重写整个文件**。
压缩后通常只有几十 KB。

压缩失败会**回退成原图**，不会因为优化把功能搞坏。

---

## 2025-11 流式文字改成每帧渲染

### 🔤 「几个字几个字往外冒」的真正原因

**渲染节流太粗。** 原来是 `RENDER_THROTTLE_MS = 50`（每秒最多重绘 20 次），
而模型每秒吐 20~60 个 token —— 每次重绘都攒下 1~4 个 token 一起蹦出来，
看起来就是"几个字几个字地闪"。

**改法：去掉时间节流，用 `requestAnimationFrame` 每帧都画**（最多 60 次/秒）。
这样每个 token 到达后最多等一帧就显示出来，文字是连续"流"出来的。

配套的两个保护：

- 仍然缓存上次渲染出的 HTML，内容没变就完全不碰 DOM。
  rAF 在没有新 token 时也会持续触发，靠这个判断避免空转重排。
- 确认了 `renderMarkdown` 是线性的一遍扫描（几个正则 + 逐行），
  几千字也就 1ms 上下；`innerHTML` 重建整棵子树约 1~2ms。
  都在 16.7ms 的帧预算内，所以每帧重绘撑得住。
  **真正贵的是读 `scrollHeight` 触发的强制同步布局**（上一版已修）。

### 🗑 顺带去掉了每个 token 一次的 DOM 查询

`onChunk` 里原来每个 token 都要
`el.messages.querySelector('.msg[data-index="N"] .msg-content')` 查一次节点。
一次流式过程中这个节点根本不会变，现在按 `requestId` 缓存起来了。

### 📉 移除了不再使用的配置

`CONFIG.RENDER_THROTTLE_MS` 已无引用，一并删除。

---

## 2025-10 流式输出不再卡顿 + 光标变细

### 🐢 「卡卡的」的四个原因，逐个修掉

**1. 平滑滚动是罪魁祸首（影响最大）**

`.messages` 上有 `scroll-behavior: smooth`。流式输出时每 50ms 就要把滚动条拽到底一次，
而 `smooth` 会让**每次赋值都启动一段几百毫秒的平滑动画**——动画还没跑完，
下一个 token 又把目标改了，于是滚动永远在"追"，看起来一顿一顿的。改成 `auto`。

**2. 定时器换成 rAF**

重绘原来用 `setTimeout(paint, 50)`，和屏幕刷新不同步，会出现「这一帧画两次、
下一帧一次不画」。改用 `requestAnimationFrame`，节流窗口内自动等下一帧。

**3. 内容没变就不碰 DOM**

加了渲染结果缓存：本次算出的 HTML 和上次一样就整段跳过。
`innerHTML` 是整棵子树重建，长回答下这是主要开销。

**4. 不再读 `scrollHeight`**

`scrollTop = scrollHeight` 里的**读取**会强制一次同步布局（reflow），
而这里每 50ms 跑一次，长对话下开销明显。
改成 `scrollTop = 1e9`（浏览器会自动夹到最大值），**只写不读**，
布局就能被推迟到下一个渲染帧。

### ✏️ 光标变细

- 宽度 `7px` → **`2px`**，高度 14 → 15px，加了 1px 圆角。
- 顺手删掉了那个 `blink` 闪烁动画：**它其实一直是失效的**。
  流式重绘是 `innerHTML` 整块替换，这个伪元素每 50ms 就被重建一次，
  动画不断从头开始，永远停在第一帧——所以你看到的一直是一块不动的方块，
  加上 7px 的宽度就显得"又宽又呆"。既然闪不了，不如老实做一根细竖条。
- 一并删除了只用在这里的 `@keyframes blink`。

---

## 2025-09 提示条不再挡输入框

- 提示条（toast）原来固定在**窗口底部居中**（`bottom: 26px`），
  而聊天输入区正好占着底部约 107px，所以每次提示都会压在输入框上。
- 现在挪到**顶栏下方居中**（`top: 86px`）：
  - 不再挡输入框
  - 也不会挡正在看的最新消息（消息列表是从下往上读的）
- 位置从 `fixed` 改成 `absolute`，并把提示条移进 `.main` 里，
  这样它是**居中于对话区**，而不是整个窗口（否则会被左侧边栏推得偏左）。
- 层级不变：提示条 `z-index: 100`，弹窗 `z-index: 50`，
  所以设置弹窗打开时提示条仍然浮在它上面。

---

## 2025-08 只沿用 Element Plus 的配色

> 中途试过把按钮和输入框完全按 `el-button` / `el-input` 的规格重做
> （32px 高、4px 直角、扁平无阴影），结果比原来难看，已经**全部回退**。
> 最后只保留了颜色：**强调色换成 Element 的主色 `#409eff`**。

### 🎨 只改了颜色

- `--accent` 从 `#4a86e8` 改成 Element 的 `#409eff`，
  配套的 `--accent-hover` / `--accent-pressed` / `--accent-soft` 同步换成
  Element 色阶里的 `light-3` `#79bbff` 和 `dark-2` `#337ecc`。
- 角色下拉框选中态的描边色跟着新主色微调。
- **除此之外，所有尺寸、圆角、间距、阴影、交互全部保持原样**：
  按钮仍是胶囊形 + 轻投影，输入框仍是 40px 高 / 9px 圆角 / 聚焦外发光，
  聊天输入区仍是 12px 圆角。

### ↩️ 回退掉的东西（记录一下，别再走一遍）

试过但已撤销：按钮 32px 高 / 4px 圆角 / 扁平无阴影、输入框 32px 高 /
聚焦只换边框色不加光晕、下拉箭头改到 8px、字段标签留白收窄、开关底色调整，
以及随之引入的一整套 `--el-*` 设计变量（已全部删除，避免留下没人用的变量）。

结论：**Element Plus 的「颜色」值得借鉴，它的「控件尺寸和形状」不适合这个界面** ——
那套是给后台管理系统设计的，塞进聊天界面会显得又小又硬。

---

## 2025-07 开发流程

### 🛠 新增 `npm run dev`

- `npm run dev`（等价于 `electron . --dev`）会自动**打开 DevTools**，
  并**监听 `renderer/` 目录，文件一保存就自动刷新窗口**。
- 改界面（`style.css` / `renderer.js` / `index.html`）从此不用重启应用，
  也不用手动按 Ctrl+R —— 跟写静态页面一样。
- `main.js` / `preload.js` 仍然需要重启，它们只在启动时读一次。
- 监听用 Node 自带的 `fs.watch`（带 150ms 防抖），**不引入任何新依赖**；
  窗口关闭时会自动停掉监听。
- 原本的 `BARBARA_OPEN_DEVTOOLS=1` 环境变量仍然有效。

---

## 2025-06 对话区间距修正

- **AI 回复没有内边距**：上一版为了「干净」把 AI 气泡的 `padding` 去掉了，
  结果文字紧贴边缘、两条回复之间也显得很挤。现在 AI 回答恢复了浅色气泡底
  （`--bg-card` + 一道极浅的描边，**不加投影**），内边距和用户气泡统一成 11×15。
- **消息间距**：列表上下留白从 22/8 调到 24/18，消息之间从 16px 加到 18px。
- **聊天输入框被撑成 104px 高（真 bug）**：输入框没有自己声明 `min-height`，
  吃到了通用 `textarea { min-height: 104px }`（那条规则本来是给设置里的大文本框用的）。
  现在显式写成 42px，输入框恢复成正常高度。

---

## 2025-05 昼夜模式与细节修正

### 🌗 新增：白天 / 夜间模式切换

- 左上角品牌名右边多了一个小图标（月亮 / 太阳），点一下就在浅色和深色之间切换。
- 选择会记进 `config.json`，**下次启动还是这个模式**。
- 为了不让深色模式启动时先闪一下白，主进程会先读设置决定窗口底色，
  并通过启动参数把主题告诉 preload，让它在**首屏渲染之前**就打上主题标记。
- 两套配色都收在同一份变量里（`:root` 是浅色，`[data-theme="dark"]` 覆盖同名变量），
  切换只改 `<html>` 上的一个属性，不碰任何具体规则。

### 🔧 间距与字重修正

- **输入框文字贴边**：左右内边距从 12px 加到 14px；底部输入区左侧从 12px 加到 15px。
- **下拉框文字顶到箭头**：改为隐藏系统原生箭头、用 CSS 自己画一个，
  右侧留出 36px（顶栏的小下拉框留 32px），文字不会再和箭头叠在一起。
  ⚠️ 这带来一个坑：`background` 简写会把箭头一起清掉，所以角色下拉框的选中态
  改用 `background-color`。
- **按钮字重**：按钮从 600 改回 400（正常字重）；小按钮、服务商标签、
  预设芯片、下拉框也从 500 统一降到 400。

---

## 2025-04 浅色主题（重做）

### 🎨 界面换成克制的浅色风

第一版浅色主题做得太花（到处是渐变、彩色发光、彩色投影，每个元素都带阴影），
看着眼花而且廉价。这一版按「少即是多」重做：

- **只保留一个强调色**：清爽的蓝，其余全是中性灰蓝。删掉了粉色调色板和所有渐变。
- **删掉所有彩色发光**：品牌名渐变文字、头像蓝粉渐变、按钮彩色光晕全部去掉，
  改成纯色 + 中性投影。
- **AI 回答不再套气泡框**：一条回复一个框、框再带投影是显乱的主因，
  现在 AI 的回答直接排在背景上，只有你的消息用淡蓝气泡区分。
- **阴影只留三个层级**，而且都很轻、统一用中性深蓝调；输入框、芯片、卡片上的投影全部去掉，
  改用极浅的边框来划分区块。
- **圆角统一**成 12 / 9 两档，不再一个元素一个尺寸。
- **去掉装饰性动画**：品牌圆点的脉动、空状态来回飘的表情都删了。
- 配色对比度提高，正文用更深的颜色，长时间看着不累。
- **左上角的品牌标识按原样保留**：音符「♪」+ 蓝粉渐变字 + 会呼吸的渐变圆点，
  参数全部还原成原来的值。品牌用自己固定的 `#5b9dff → #ff9fc8`，
  不跟随主题的强调色，所以不受后续调色影响。

---

## 2025-03 角色扮演支持

### ✨ 新功能：角色库 / 角色卡

- **导入酒馆（SillyTavern）角色卡**：直接选 `.png` 文件，程序会解析 PNG 里的 tEXt 数据块
  （关键字 `chara` / `ccv3`），把角色设定、头像、开场白、示例对话全读进来。也支持 `.json` 角色卡（v1 / v2 / v3 三种格式）。
- **内置角色编辑器**：可以完全手写一个角色，字段包括名称、描述、性格、场景、开场白、
  示例对话、角色专属系统提示词、对话后指令、作者备注、标签。
- **每个会话独立绑定角色**：A 会话扮演芭芭拉、B 会话扮演雷电将军，互不影响。
  新建对话会继承当前会话的角色。
- **顶部角色切换**：聊天窗口右上角第一个下拉框，选完立刻生效。
- **开场白自动插入**：空对话绑上带开场白的角色时，自动把开场白作为角色的第一句话放进去。
- **`{{char}}` / `{{user}}` 宏替换**：角色卡里可以放心用这些占位符，也包括 `<BOT>` / `<USER>`。
- **示例对话注入**：角色卡的 `mes_example` 会被解析成真正的 user/assistant 消息一起发给模型
  —— 这是提升扮演质量最有效的一招。
- **可调上下文轮数**：新增「带入轮数」设置（默认 20，最大 200），替掉了原来写死的 20 轮。
  角色扮演建议调大到 40～60。
- **「你的名字」设置**：决定 `{{user}}` 替换成什么。
- **角色头像**：导入的卡片头像会显示在角色列表和每条 AI 消息的旁边。

### 🐛 复查后修复的问题

- **用户自己的消息会顶角色头像**（我重构时引入的回归）：绑了带头像的角色后，
  你自己发的气泡上会出现角色的脸，错误提示也显示头像而不是 `!`。已按消息角色区分。
- **示例对话里的宏没被替换**：正文中的 `{{user}}` / `{{char}}` 会以字面量发给模型。
  现在和系统提示词、历史消息走同一套替换。
- **空名字校验失效**：校验发生在写入内存之后，导致弹了提示但角色已被改名成「未命名角色」。
- **开场白识别过宽**：原来靠「只有一条 assistant 消息」判断，删掉自己的提问后，
  留下的真实回复会被当成开场白替换掉。现在给自动插入的开场白打 `greeting` 标记，只替换带标记的。
- **旧版 `char_*` 角色卡丢字段**：补上 `char_persona` / `char_greeting` /
  `char_example_dialogue` / `world_scenario` 的兼容映射。
- **独立 JSON 卡的头像丢失**：v2/v3 卡的头像在 `data.avatar` 里，原来只读顶层。
- **PNG 头像可能被静默丢弃**：导入上限 40MB 但头像上限只够约 4.4MB 的图，
  超出的会被悄悄丢掉。现在导入上限 12MB、头像上限放宽到 18M 字符，两者对得上，不会再丢。
- **角色名含 `$` 时替换错乱**：`String.replace` 的字符串参数会把 `$&`、`$1` 当特殊写法，
  改用函数式替换。

### 🔄 行为变更（重要）

- **绑定了角色的会话不再使用「设置」里的全局人设**，改用角色卡自己的设定。
  这是故意的：否则扮演某个角色时，全局人设还在说「你是芭芭拉」，模型会精神分裂。
  角色卡里留空「角色专属系统提示词」时，会用一份通用扮演规则代替。
- 绑定时会自动追加一段「扮演规则」，要求模型保持人设、不跳出角色、不提自己是 AI。

### 🛡 数据安全

- 删除角色时会自动把用到它的会话解绑，不会留下悬空的引用。
- 角色数据存在 `%APPDATA%\Barbara\characters.json`，和会话一样有 `.backup` 兜底。
- 旧的 Cyrene 数据目录迁移列表里加上了 `characters.json`。

---

## 2025-02 多模型支持

### ✨ 新功能：多模型 / 多服务商

- **可以同时配置多个服务商**：设置 →「模型服务」→「＋ 添加服务商」，
  内置 DeepSeek / OpenAI / 通义千问 / 智谱 GLM / Kimi 五个预设，一键填好接口地址和常用模型。
- **聊天窗口右上角切换模型**：下拉框按服务商分组列出全部模型，选中立刻生效，不用进设置。
- **每个会话记住自己的模型**：A 会话用 gpt-4o、B 会话用 deepseek-chat，互不影响；
  标题下方显示当前会话在用什么。
- **每条回答标注模型名**：AI 消息左上角的小标签显示是哪个模型答的，方便对比。
- **拉取模型列表**：点「拉取可用模型」从服务商接口拉取完整模型列表并自动填入（最多 120 个）。
- **每个服务商的 Key 独立加密存储**，与之前一样使用系统 safeStorage。

### 🔄 兼容性

- **旧的单服务商配置会自动升级**：原来的 `baseUrl` / `apiKey` / `model` 会被装进
  「默认服务商」，设置和 Key 都不会丢。
- 老会话没有记录模型，会自动回落到当前默认模型，不会报错。

### 🛠 顺带修复

- 修复了旧版配置在升级时可能被误判为新格式、导致 API Key 丢失的问题。
- 「测试连接」和「拉取可用模型」现在针对**当前编辑的服务商**，而不是全局那一套。

---

## 2025-01 安全与稳定性优化

### 🔒 安全改进

1. **API Key 加密存储**
   - 使用 Electron `safeStorage` API 加密存储 API Key
   - 自动兼容旧版本明文配置，首次加载后自动加密
   - 加密失败时回退到明文存储（向后兼容）

2. **数据备份与恢复**
   - 每次写入 config.json 和 conversations.json 前自动创建 .backup 备份
   - 主文件损坏时自动从备份恢复
   - 防止因意外崩溃或磁盘错误导致数据完全丢失

3. **并发写入保护**
   - 实现写入队列，串行化所有文件写入操作
   - 避免多个保存操作同时写入导致的数据竞态和损坏

### 📊 数据管理

4. **会话数量限制**
   - 限制最多保存 100 个会话（按更新时间保留最近的）
   - 每个会话最多保留 200 条消息
   - 防止 conversations.json 无限增长导致性能下降

5. **改进的错误处理**
   - 保存失败时向用户显示提示
   - 关键错误不再静默吞掉
   - 更友好的错误消息

### 🎨 用户体验改进

6. **优化"打开数据文件夹"按钮**
   - 从"高亮 config.json"改为"直接打开数据文件夹"
   - 更符合按钮标签的语义

7. **参数验证**
   - temperature 限制在 0-2 范围
   - maxTokens 限制在 64-32000 范围
   - 防止无效参数导致 API 调用失败

8. **代码质量提升**
   - 提取魔法数字为配置常量（CONFIG 对象）
   - 提高可维护性和可读性
   - 更清晰的代码注释

### 📝 文档更新

9. **修复使用说明中的路径问题**
   - 移除硬编码的绝对路径
   - 改为占位符或相对路径说明

### ⚠️ 注意事项

- **首次运行优化版本**：现有的明文 API Key 会被自动加密，这是正常行为
- **备份文件**：数据目录中会出现 `.backup` 文件，请勿手动删除
- **会话清理**：如果你有超过 100 个会话，最旧的会话会被自动归档（删除）
- **兼容性**：完全向后兼容，可以直接升级而不丢失数据

### 🔧 技术细节

- 增强了 `writeJson()` 函数的健壮性
- 新增 `loadJsonWithFallback()` 函数支持备份恢复
- 新增 `encryptApiKey()` 和 `decryptApiKey()` 函数
- 重构配置常量管理（renderer.js 中的 CONFIG 对象）

---

如发现任何问题，请查看数据目录中的 `.backup` 文件进行手动恢复。
